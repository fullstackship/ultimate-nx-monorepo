describe('Core module generic test', () => {
  it('Should be true', () => {
    expect(true).toBeTruthy();
  });
});

/*
import { NxBaseCoreModule } from './core.module';

describe('NxBaseCoreModule', () => {
  it('should work', () => {
    expect(new NxBaseCoreModule(null)).toBeDefined();
  });
});
*/
